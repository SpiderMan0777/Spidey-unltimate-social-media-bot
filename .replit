# Define the entry point for your project
entrypoint = "bot.py"

# Configure deployment settings
[deployment]
run = ["python3", "bot.py"]

# Optionally, specify environment variables
[run.env]
# Replace with your actual bot token or other environment variables
BOT_TOKEN = "your-telegram-bot-token"
